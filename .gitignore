# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# tf
tf/service/.terraform
tf/service/.terraform.lock.hcl
tf/service/terraform.tfstate
tf/service/terraform.tfstate.backup
tf/service/service.tfplan
tf/devel/.terraform
tf/devel/.terraform.lock.hcl
tf/devel/terraform.tfstate
tf/devel/terraform.tfstate.backup
tf/devel/devel.tfplan

# VSCode
.vscode

# secrets
tgbot.token